server:
  port: 8080

spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  main:
    allow-bean-definition-overriding: true

javainuse:
  rabbitmq:
    exchange: weikun.exchange
    queue: weikun.queue
    routingkey: weikun.routingkey